



spring:
  cloud:
    inetutils:
      preferred-networks:
        - 192.168
#    consul:
#      discovery:
#        hostname: 192.168.8.225
#        prefer-ip-address: false
  cache:
    caffeine:
      spec: maximumSize=5000,expireAfterWrite=5s #expireAfterAccess 一定时间内没有读写 , expireAfterWrite 一定时间内没有创建/覆盖时 , refreshAfterWrite 指定时间内没有被创建/覆盖

logging:
  file:
    name: logs/${project.artifactId}.log
#  level:
#    com:
#      github:
#        microservice:
#          gateway:
#            core: debug
#    org:
#      springframework:
#        cloud:
#          netflix: trace
#          gateway: trace
#debug: true
#trace: true